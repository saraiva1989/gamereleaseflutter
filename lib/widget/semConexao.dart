import 'package:flutter/material.dart';

Widget semConexao() {
    return Container(
      alignment: Alignment.center,
      child: Text("without internet connection!", style: TextStyle(fontSize: 25),),
    );
  }